# P07: Native Tickets service and optional sibling adapters

Continue after your ontology receipt. Read IMPLEMENTATION_PLAN.md, examples/lean/Examples/Tickets/Domain.lean, and relevant ../leandb_v2 and ../leanhttp code. Own ONLY examples/lean/Examples/Tickets/Contracts.lean, examples/native/, tests/native/, and docs/NATIVE.md. Parent owns the domain, components, root config, browser adapter. Do not edit sibling repositories.

Implement native shared Tickets public operation codecs using your ontology/contract API: list and revision-checked save. Export explicit public operation manifest and wire request/response format. Keep browser EntityId separate from SQLite row IDs. Define storage flat record + mapping to public TicketSummary; use LeanDB's existing APIs for persistence. Handler must detect not-found and revision conflicts using current persisted record. Preserve large integers with canonical string encoding before JSON serialization. Use local package dependencies on root and sibling repos under examples/native; keep native-only imports away from portable domain. Build against cached sibling dependencies; if network/escalation needed report exact command to parent and finish all independent work.

Implement a small local HTTP server using Lean Std.Http with /api/tickets/list and /api/tickets/save (or one /api/call with explicit operation identity; document your actual routes for parent). Body JSON and typed errors, contract version mismatch rejection, no arbitrary DB argv execution. Server accepts port and SQLite file path argv. Existing LeanDB.Http is a useful adapter pattern. Avoid holding SQLite connection across async concurrent work without serialization. Parent can run server/browser once built.

Demonstrate LeanHttp through a local fixture server request with correct domain error status decoding (no external credentials). A native integration test should drive list, successful save, stale save, invalid input, large revision round-trip, changed client contract. Prefer executable fixture protocol + a parent-run script. Include exact startup/test/build commands in docs/NATIVE.md. Record any concrete upstream extraction proposals supported by the adapter. Do not overreach into sibling core changes.

Safe envelope: no commits/pushes/force-pushes; no deleting untracked/ignored files; no secrets/.env; no network/spending/publishing/external messages; no edits outside owned paths; no agents. If native dependency build writes sibling .lake and sandbox blocks, parent will run the necessary build with authorization; do not replace real integration with stubs.
